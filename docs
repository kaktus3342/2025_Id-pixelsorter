1) On a d’abord l’importation de toutes les données relatives au projet et des fichiers.
2) Ensuite, on définit toutes les fonctions qu’on utilisera après
3) Début de la boucle jeu et changements de scène lorsque l’on clique sur un bouton
Le fichier fond2.png est le fond que l’on a créé par nous-mêmes.
